<h1 id="pagetitle">New site template</h1>

<%= error_messages_for :the_template %>

<% form_for(:the_template, :url => site_templates_path, :html => { :multipart => true }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
<% end %>

<%= link_to 'Back', site_templates_path %>